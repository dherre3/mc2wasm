module("matwably") {
    java {
        basedir "."
        include "main/**/*.java"
        include "gen/**/*.java"
    }

    jastadd {
        include "src/spec/*.ast"
        include "src/spec/*.jadd"
        include "src/spec/*.jrag"
    }
}