module("matwably") {
    java {
        basedir "src/main/java/"
        include "**/*.java"
        include "**/*.java"
    }

    jastadd {
        include "src/spec/*.ast"
        include "src/spec/*.jadd"
        include "src/spec/*.jrag"
    }
}